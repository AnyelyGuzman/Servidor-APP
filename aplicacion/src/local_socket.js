// const net = require('net');

// const client = new net.Socket();

// client.connect(52275, '127.0.0.1', () => { console.log('Socket connect'); });

// client.on('close', () => console.log('Connection closed'));

// export { client }
